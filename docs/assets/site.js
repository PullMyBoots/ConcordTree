const translations = {
  zh: {
    navAccuracy: "准确性",
    navPerformance: "性能",
    navAppendix: "附录",
    navMethod: "方法",
    accuracyTitle: "准确性测试",
    speciesTitle: "物种树",
    speciesSource: '<a href="https://github.com/adamallo/SimPhy">SimPhy 1.0.2</a> 与 AliSim 模拟的 100 基因座数据；Small 为 32–256 个物种，Large 为 512–4096 个物种。',
    figSpecies: "图 1.",
    figSpeciesCaption: "SimPhy Small 与 Large 上的平均 nRF（95% bootstrap 置信区间）。",
    geneTitle: "基因树",
    geneSource: '<a href="https://iqtree.github.io/doc/AliSim">AliSim</a> 模拟的 200 条 DNA 比对，覆盖 256–4096 个物种。',
    figGene: "图 2.",
    figGeneCaption: "AliSim 基因树数据上的平均 nRF（95% bootstrap 置信区间）。",
    realTitle: "真实数据与 DIPPER 数据",
    empiricalTitle: "已发表真实数据",
    dataset: "数据来源",
    datasetShort: "数据",
    attention: "注意力",
    lightweight: "轻量",
    mean: "平均",
    dipperTitle: "DIPPER 论文模拟数据",
    dipperSource: 'DIPPER 论文公开的 AliSim 10K–50K 与 RNASim 10K–500K 数据（<a href="https://doi.org/10.1038/s43588-026-01015-8">Walia et al., 2026</a>）。',
    taxa: "物种数",
    performanceTitle: "性能测试",
    performanceSource: "NVIDIA L40S 上的实测时间；每个物种规模包含 10 条 AliSim 数据。",
    figRuntime: "图 3.",
    figRuntimeCaption: "每条 MSA 的中位运行时间；阴影为四分位区间。",
    rnasimRuntimeTitle: "RNASim 大规模运行时间",
    rnasimRuntimeSource: "同一批 DIPPER 论文 RNASim 数据，在单张 NVIDIA L40S 上运行。",
    runtimeMultiple: "耗时倍数",
    total: "合计",
    rnasimRuntimeNote: "两种方法均按下述测试配置运行；表中报告单张 NVIDIA L40S 上的完整算法时间。",
    appendixTitle: "附录：数据与测试配置",
    appendixSpeciesTitle: "A.1 物种树模拟",
    appendixSpeciesP1: '<a href="https://pmc.ncbi.nlm.nih.gov/articles/PMC4748750/">SimPhy 1.0.2</a> 为每条记录生成一棵物种树和 100 棵基因树。ILS 组的基因树差异来自不完全谱系排序；complex 组在此基础上加入基因复制、基因丢失和水平转移。随后使用 <a href="https://pmc.ncbi.nlm.nih.gov/articles/PMC9113491/">AliSim</a> 沿每棵基因树模拟 DNA 序列。每个基因座为每个物种保留一条代表序列，缺失的基因座以缺口表示，再将 100 个基因座连接为一个超矩阵。评测时以生成的物种树为参考。',
    appendixSpeciesP2: "Small 数据包含 32、64、128、192 和 256 个物种，每个规模各有 50 条 ILS 和 50 条 complex 比对，共 500 条。Large 准确性图使用 512 和 1,024 个物种的共同数据，每个规模同样包含两种条件各 50 条，共 200 条。",
    parameter: "参数",
    taxonScales: "物种数",
    speciationRate: "物种形成率",
    speciesTreeHeight: "物种树高度",
    individualsPerSpecies: "每物种个体数",
    effectivePopulation: "有效群体大小",
    generationTime: "世代时间",
    substitutionRate: "全树替换率",
    rateHeterogeneity: "分支替换率异质性",
    duplicationLoss: "基因复制与丢失",
    horizontalTransfer: "水平转移",
    absent: "不模拟",
    distanceDependent: "受供体—受体进化距离约束",
    sequenceModel: "序列演化模型",
    optionalInvariant: "可选 +I；参数由经验拟合库逐基因座抽取",
    locusLength: "单基因座序列长度",
    raxmlGroveSource: 'AliSim 使用的逐基因座序列长度、缺口比例、碱基频率、替换率、Γ 形状参数和不变位点比例来自 <a href="https://github.com/angtft/RAxMLGrove">RAxML Grove</a> 经验参数库。该开放数据库汇集了 RAxML 与 RAxML-NG Web 服务器上真实分析任务的匿名化模型估计（<a href="https://doi.org/10.1093/bioinformatics/btab863">Höhler et al., 2022</a>）。',
    simphyNotation: "Lognormal 后两个数字分别为对数尺度的位置与尺度参数；GB、GT、LD、LB 和 LT 为 SimPhy 对基因复制、转移、丢失及其逐基因座速率的参数记号。",
    appendixGeneTitle: "A.2 基因树模拟",
    appendixGeneP1: '<a href="https://pmc.ncbi.nlm.nih.gov/articles/PMC9113491/">AliSim 2.4.0</a> 直接在一棵随机树上模拟一条 DNA 比对，因此每条比对中的全部位点共享同一进化历史。生成树同时作为该条记录的参考树。',
    appendixGeneP2: "数据覆盖 256、512、1,024、2,048 和 4,096 个物种，每个规模 40 条，共 200 条。树形包括 Yule-Harding、均匀随机、平衡树和阶梯树；序列使用多组 GTR 核苷酸替换参数、碱基频率和位点速率异质性，根序列长度为 2,048–16,384 个位点，并在部分记录中模拟插入和缺失。",
    appendixDipperTitle: "A.3 DIPPER 论文数据",
    appendixDipperP1: '该组数据直接采用 <a href="https://doi.org/10.1038/s43588-026-01015-8">DIPPER 论文</a>公开的比对和生成树。A10、A20 和 A50 是 AliSim 生成的 DNA 数据；R10、R20、R50、R100、R200 和 R500 是 <a href="https://arxiv.org/abs/0912.2326">RNASim</a> 生成的 RNA 数据，覆盖 10K–500K 个物种。',
    appendixConfigTitle: "A.4 测试配置",
    appendixConfigP1: "ConcordTree 使用 4 个视图。表格中的“轻量”和“注意力”仅表示四元组评分器的两种可选实现，其余推断流程一致。局部优化以交换比例 0.005 为停止阈值；若拓扑进入重复循环，则保留循环中评分最优的树并停止。",
    appendixConfigP2: "DIPPER 论文数据使用适合其稀疏且相干覆盖结构的缺失感知距离，其余面板使用标准距离。DIPPER GPU 按论文配置运行。",
    methodAppendixTitle: "附录：方法",
    methodOverviewTitle: "B.1 方法概览",
    methodParametersTitle: "推断参数",
    methodParametersIntro: '令 <em>X</em>∈Σ<sup><em>N</em>×<em>L</em></sup> 表示包含 <em>N</em> 个物种和 <em>L</em> 个对齐位点的 MSA。ConcordTree 的输出是与输入具有相同叶集的一棵无根二叉树 <em>T</em>。一次推断由下列参数组确定：',
    parameterTreeTarget: "任务类型",
    parameterTreeTargetText: "可选基因树或物种树，默认为物种树。",
    parameterDistance: "AP-NJ 距离",
    parameterDistanceText: "可选标准距离或缺失感知距离，默认为标准距离。",
    parameterScorer: "四元组评分器",
    parameterScorerText: "对从一条内部边的四个相邻分支各选一个物种形成的四元组，评分器分别为 AB|CD、AC|BD 和 AD|BC 给出支持分数。分数越高，表示该拓扑与 MSA 中的序列模式越相容；NNI 使用候选拓扑相对当前拓扑的分数增益。可选轻量 MLP 或 Transformer，默认为 MLP。View NNI 始终使用与任务类型匹配的 MLP。",
    parameterViews: "View 数量",
    parameterViewsText: "可设为 2–8，默认为 4。每个 View 选择一个互补位点子集，并最终产生一棵包含全部 N 个物种的树。",
    parameterStops: "精修控制",
    parameterStopsText: '对阶段 <em>z</em>∈{View, Coordinate, Saturation}，ε<sub>z</sub> 是交换比例阈值，<em>R</em><sub>z</sub> 是最大轮数。三个阶段的默认值依次为 (0.01,24)、(0.005,4) 和 (0.005,5)。',
    methodApnjTitle: "AP-NJ：无矩阵聚合轮廓邻接法",
    apnjIntro: '第 <em>k</em> 个 View 选择位点集合 <em>J</em><sub>k</sub>⊆{1,…,<em>L</em>}，但始终保留全部 <em>N</em> 个物种。AP-NJ 在该子矩阵上构造一棵完整骨架树。每个物种或已合并簇在位点 <em>s</em> 上由四维状态轮廓表示；观测碱基对应 one-hot 向量，缺失状态由距离模型 <em>g</em> 处理。',
    apnjStandardText: '在标准模型中，缺失状态使用该位点的经验碱基频率 <em>q</em><sub>s</sub> 表示。若 <em>r</em><sub>is</sub> 和 <em>r</em><sub>js</sub> 是两个簇的状态轮廓，则位点不匹配距离为',
    apnjStandardExplain: "内积表示两个轮廓在该位点取相同碱基的概率，因此一减去该值就是期望不匹配概率。",
    apnjCoverageText: '在缺失感知模型中，<em>p</em><sub>is</sub> 对观测碱基取 one-hot 向量，对缺失单元取零向量；<em>m</em><sub>is</sub>=<strong>1</strong><sup>T</sup><em>p</em><sub>is</sub> 是观测质量。<em>q</em><sub>s</sub> 是位点 <em>s</em> 上已观测碱基的经验分布，π<sub>s</sub>=1−‖<em>q</em><sub>s</sub>‖<sub>2</sub><sup>2</sup> 是从该分布独立抽取两个碱基时的不匹配概率。令 <em>c</em><sub>s</sub> 为该位点的观测物种比例，<em>w</em><sub>s</sub>=<em>c</em><sub>s</sub><sup>2</sup> 是随机物种对都在该位点被观测的概率。定义',
    apnjCoverageFormulaLead: "其中 h_is 表示已观测状态相对于背景不匹配率的偏移。则缺失感知的位点距离为",
    apnjCoverageExplain: "当两端均被观测时，该式退化为普通不匹配；当两端均缺失时，距离回到位点先验；只有一端被观测时，它在边际化和经验填补之间连续插值。",
    csiTitle: "相干稀疏指数",
    csiIntro: 'CSI 同时描述物种对的共同观测稀疏度，以及不同物种是否在相同区域缺失。令 <em>S</em><sub>i</sub>为物种 <em>i</em> 具有实际观测的位点集合。对每一对物种，定义共同观测比例 <em>O</em><sub>ij</sub> 和观测掩码的 Jaccard 相似度 <em>J</em><sub>ij</sub>：',
    csiDefinitionLead: "相干稀疏指数（coherent sparsity index, CSI）定义为",
    csiExplain: "第一项在物种对能够共同观测的位点较少时增大，表示“稀疏性”；第二项在不同物种的观测区域高度重合时增大，表示“相干性”。只有两者同时出现，CSI 才高。因此，CSI 区分了普通的分散缺失与成块、相关的缺失，后者正是缺失感知距离所建模的情形。",
    csiGuidance: "CSI≤0.25：选择标准距离。CSI≥0.35：选择缺失感知距离。0.25<CSI<0.35：比较两种距离。",
    apnjAggregateText: '两种距离对簇轮廓都是仿射双线性的。令当前活动簇集合为 <em>A</em>、簇数为 <em>n</em><sub>A</sub>，聚合轮廓为 <em>P</em><sub>s</sub>=∑<sub><em>j</em>∈<em>A</em></sub><em>p</em><sub>js</sub>。算法可以直接由 <em>P</em><sub>s</sub> 得到标准 NJ 行和 <em>R</em><sub>i</sub>=∑<sub><em>j</em>∈<em>A</em></sub><em>d</em><sub>g</sub>(<em>i</em>,<em>j</em>)，无需建立稠密的 <em>N</em>×<em>N</em> 距离矩阵。对于稀疏候选对，仍使用标准 NJ 准则',
    apnjQExplain: "其中 d_g(i,j) 是参数 g 选择的簇间距离，R_i 和 R_j 是对应的距离行和。AP-NJ 只稀疏化候选对的搜索；候选对的距离、行和与 Q 值均按选定公式计算。",
    algorithmLabel: "算法",
    apnjAlgorithmIO: '<strong>输入：</strong>View 子矩阵 <em>X</em><sub>J</sub>，距离模型 <em>g</em>。<strong>输出：</strong>包含全部物种的二叉树。',
    apnjAlgorithmSteps: '<li>为每个物种初始化状态轮廓，并令活动簇集合 <em>A</em> 为全部叶节点。</li><li>当 |<em>A</em>|&gt;3 时，利用轮廓投影为每个簇提出少量近邻候选。</li><li>由聚合轮廓计算所有活动簇的精确 NJ 行和 <em>R</em><sub>i</sub>。</li><li>对候选对计算 <em>Q</em>(<em>i</em>,<em>j</em>)，选择互为最佳且彼此不重叠的簇对。</li><li>并行合并选中的簇对；更新平均轮廓和距离偏移，使后续距离满足标准 NJ 递推式。</li><li>连接最后三个活动簇并返回树。</li>',
    methodMergeTitle: "相容分支合并",
    mergeIntro: '每棵 View 树 <em>T</em><sub>k</sub> 被写成非平凡二分分支集合 <em>S</em>(<em>T</em><sub>k</sub>)。算法首先建立分支库 𝒮=⋃<sub><em>k</em>=1</sub><sup><em>K</em></sup><em>S</em>(<em>T</em><sub>k</sub>)。对分支 <em>s</em>，其 View 支持数为',
    mergeSupportExplain: "c(s)=K 表示所有 View 一致支持该分支。其余分支按局部神经收益是否在所有可用 View 中为非负、支持数、收益中位数和均值依次排序。",
    mergeCompatibilityText: '两个分支 <em>A</em>|<em>A</em><sup>c</sup> 和 <em>B</em>|<em>B</em><sup>c</sup> 相容，当且仅当下面四个交集至少有一个为空：',
    mergeCompatibilityExplain: "这个条件保证两个二分分支能够同时出现在同一棵无根树中。算法按证据顺序依次接收相容分支，因此得到的是该顺序下的极大相容集合，而不是声称求解全局加权最优树。",
    mergeMedoidText: "为了给尚未解析的区域提供一致锚点，定义中心 View 为",
    mergeMedoidExplain: "其中 △ 表示对称差，其大小就是两棵树不同的分支数。中心 View 因此是与其余 View 总分支差最小的那棵树；它只补足锚点，不覆盖已接受的相容分支。",
    mergeAlgorithmName: "相容分支合并",
    mergeAlgorithmIO: '<strong>输入：</strong>View 树 <em>T</em><sub>1</sub>,…,<em>T</em><sub>K</sub> 及其分支证据。<strong>输出：</strong>共识树 <em>T</em><sub>0</sub>。',
    mergeAlgorithmSteps: '<li>提取全部 View 树的规范化二分分支并建立分支库。</li><li>将所有 c(s)=K 的一致分支加入集合 𝒞。</li><li>按稳定性、View 支持数和神经证据对其余分支排序。</li><li>依次扫描分支；若候选分支与 𝒞 中全部分支相容，则将其加入 𝒞。</li><li>继续加入中心 View 中与 𝒞 相容的锚点分支。</li><li>确定性补全 𝒞，直到包含 N−3 个内部边，并将其实体化为 T₀。</li>',
    methodNniTitle: "三种 NNI 精修",
    nniIntro: '对于内部边 <em>e</em>，其四个相邻分支记为 <em>A</em>、<em>B</em>、<em>C</em> 和 <em>D</em>。保持当前拓扑与两种 NNI 交换构成三个候选状态：AB|CD、AC|BD 和 AD|BC。四元组评分器给出势函数',
    nniGainText: '其中 <em>m</em> 是所选评分器。若当前状态为 τ<sub>0</sub>，最佳替代状态为 τ<sup>*</sup>，则交换收益为 Δ(<em>e</em>)=<em>L</em><sub>m</sub>(τ<sup>*</sup>)−<em>L</em><sub>m</sub>(τ<sub>0</sub>)。每轮只执行收益满足该阶段准则且互不共享内部节点的 NNI，以避免同时修改相邻边。',
    viewNniText: "在每棵 AP-NJ 树上检查全部内部边。树的初始拓扑来自该 View 的位点子集，但 NNI 评分读取完整 MSA。它从四个相邻分支各取少量邻近代表，用轻量评分器消除明显局部错误。",
    coordinateNniText: "从 T₀ 开始，只检查没有得到全部 View 一致支持的边。一个上下文面板可以服务多条邻近边，每条边由两个覆盖视角共同确认，从而批量修复共识树。",
    saturationNniText: "在 Coordinate 输出树上重新识别不确定边，并为每条边建立专属且四向均衡的上下文。它要求整体收益和代表四元组的中位收益均为正，用于集中复查剩余边。",
    nniStopText: '设第 <em>r</em> 轮接受的交换集合为 <em>M</em><sub>r</sub>。一棵含 <em>N</em> 个叶的无根二叉树具有 <em>N</em>−3 条内部边，因此定义归一化交换比例',
    nniStopExplain: '阶段 <em>z</em> 在没有可接受交换、ρ<sub>r</sub>≤ε<sub>z</sub> 或 <em>r</em>≥<em>R</em><sub>z</sub> 时停止。ε<sub>z</sub> 控制收敛程度，<em>R</em><sub>z</sub> 控制最坏运行预算；两者可单独禁用其一，但不能同时禁用。',
    nniAlgorithmName: "统一 NNI 精修",
    nniAlgorithmIO: '<strong>输入：</strong>树 <em>T</em>、阶段 <em>z</em>、评分器 <em>m</em>、停止参数 (ε<sub>z</sub>,<em>R</em><sub>z</sub>)。<strong>输出：</strong>精修后的树。',
    nniAlgorithmSteps: '<li>若 z=View，令目标集合为 T 的全部内部边；否则令目标集合为当前树中未获全部 View 一致支持的边。</li><li>按照阶段 z 的规则构造代表物种上下文，并计算每条目标边三种局部拓扑的势函数。</li><li>从正收益候选中按收益排序，选择一组互不冲突的 NNI。</li><li>同时应用选中的 NNI，得到下一轮树并计算交换比例 ρ<sub>r</sub>。</li><li>若没有交换、ρ<sub>r</sub>≤ε<sub>z</sub> 或达到 <em>R</em><sub>z</sub>，则返回当前树；否则重新构造上下文并继续下一轮。</li>',
    nniSchedule: '<strong>执行顺序：</strong>每棵 AP-NJ 树先运行 View NNI，得到 T₁,…,T<sub>K</sub>；合并得到 T₀ 后，依次运行 Coordinate NNI 和 Saturation NNI，输出最终树 T*。',
    localOnly: "本地预览 · 尚未发布"
  },
  en: {
    navAccuracy: "Accuracy",
    navPerformance: "Runtime",
    navAppendix: "Appendix",
    navMethod: "Method",
    accuracyTitle: "Accuracy",
    speciesTitle: "Species trees",
    speciesSource: '100-locus datasets simulated with <a href="https://github.com/adamallo/SimPhy">SimPhy 1.0.2</a> and AliSim; Small contains 32–256 taxa and Large contains 512–4096 taxa.',
    figSpecies: "Figure 1.",
    figSpeciesCaption: "Mean nRF on SimPhy Small and Large (95% bootstrap confidence intervals).",
    geneTitle: "Gene trees",
    geneSource: 'Two hundred DNA alignments simulated with <a href="https://iqtree.github.io/doc/AliSim">AliSim</a>, spanning 256–4096 taxa.',
    figGene: "Figure 2.",
    figGeneCaption: "Mean nRF on the AliSim gene-tree datasets (95% bootstrap confidence intervals).",
    realTitle: "Empirical and DIPPER datasets",
    empiricalTitle: "Published empirical datasets",
    dataset: "Dataset source",
    datasetShort: "Dataset",
    attention: "attention",
    lightweight: "lightweight",
    mean: "Mean",
    dipperTitle: "DIPPER simulations",
    dipperSource: 'Public AliSim data at 10K–50K taxa and RNASim data at 10K–500K taxa from the DIPPER paper (<a href="https://doi.org/10.1038/s43588-026-01015-8">Walia et al., 2026</a>).',
    taxa: "Taxa",
    performanceTitle: "Runtime",
    performanceSource: "Measured on an NVIDIA L40S; each taxon scale contains ten AliSim datasets.",
    figRuntime: "Figure 3.",
    figRuntimeCaption: "Median runtime per MSA; bands show interquartile ranges.",
    rnasimRuntimeTitle: "Large-scale RNASim runtime",
    rnasimRuntimeSource: "The same RNASim datasets released with the DIPPER paper, measured on one NVIDIA L40S.",
    runtimeMultiple: "Runtime ratio",
    total: "Total",
    rnasimRuntimeNote: "Both methods use the evaluation configuration described below; the table reports complete algorithm time on one NVIDIA L40S.",
    appendixTitle: "Appendix: data and evaluation settings",
    appendixSpeciesTitle: "A.1 Species-tree simulations",
    appendixSpeciesP1: '<a href="https://pmc.ncbi.nlm.nih.gov/articles/PMC4748750/">SimPhy 1.0.2</a> generated one species tree and 100 gene trees for each record. Gene-tree discordance in the ILS condition arose from incomplete lineage sorting; the complex condition additionally included gene duplication, gene loss, and horizontal transfer. <a href="https://pmc.ncbi.nlm.nih.gov/articles/PMC9113491/">AliSim</a> then simulated DNA sequences along each gene tree. One representative sequence per species was retained at each locus, absent loci were represented by gaps, and the 100 loci were concatenated into a supermatrix. The generating species tree served as the reference.',
    appendixSpeciesP2: "Small contains 32, 64, 128, 192, and 256 taxa, with 50 ILS and 50 complex alignments at each scale (500 total). The Large accuracy panel uses the common 512- and 1,024-taxon data, again with 50 alignments per condition and scale (200 total).",
    parameter: "Parameter",
    taxonScales: "Taxa",
    speciationRate: "Speciation rate",
    speciesTreeHeight: "Species-tree height",
    individualsPerSpecies: "Individuals per species",
    effectivePopulation: "Effective population size",
    generationTime: "Generation time",
    substitutionRate: "Tree-wide substitution rate",
    rateHeterogeneity: "Branch-rate heterogeneity",
    duplicationLoss: "Gene duplication and loss",
    horizontalTransfer: "Horizontal transfer",
    absent: "Not simulated",
    distanceDependent: "donor–recipient choice depends on evolutionary distance",
    sequenceModel: "Sequence-evolution model",
    optionalInvariant: "optional +I; parameters sampled per locus from an empirical fit library",
    locusLength: "Sequence length per locus",
    raxmlGroveSource: 'Per-locus sequence length, gap proportion, base frequencies, substitution rates, Γ shape, and invariant-site proportion used by AliSim come from the <a href="https://github.com/angtft/RAxMLGrove">RAxML Grove</a> empirical parameter collection. This open database contains anonymized model estimates from empirical analyses submitted to the RAxML and RAxML-NG web servers (<a href="https://doi.org/10.1093/bioinformatics/btab863">Höhler et al., 2022</a>).',
    simphyNotation: "The two Lognormal values are the location and scale on the log scale. GB, GT, LD, LB, and LT are SimPhy parameters for duplication, transfer, loss, and their locus-specific rates.",
    appendixGeneTitle: "A.2 Gene-tree simulations",
    appendixGeneP1: '<a href="https://pmc.ncbi.nlm.nih.gov/articles/PMC9113491/">AliSim 2.4.0</a> simulated each DNA alignment directly along one random tree, so all sites in an alignment share a single evolutionary history. The generating tree served as the reference for that record.',
    appendixGeneP2: "The dataset contains 40 alignments at each of 256, 512, 1,024, 2,048, and 4,096 taxa (200 total). Tree shapes include Yule-Harding, uniform random, balanced, and caterpillar topologies. The simulations span multiple GTR substitution parameters, nucleotide frequencies, and patterns of among-site rate variation, with root-sequence lengths from 2,048 to 16,384 sites; a subset also includes insertions and deletions.",
    appendixDipperTitle: "A.3 DIPPER paper datasets",
    appendixDipperP1: 'These comparisons use the alignments and generating trees released with the <a href="https://doi.org/10.1038/s43588-026-01015-8">DIPPER paper</a>. A10, A20, and A50 are DNA datasets produced with AliSim; R10, R20, R50, R100, R200, and R500 are RNA datasets produced with <a href="https://arxiv.org/abs/0912.2326">RNASim</a>, spanning 10K–500K taxa.',
    appendixConfigTitle: "A.4 Evaluation settings",
    appendixConfigP1: "ConcordTree uses four views. The lightweight and attention columns differ only in the quartet scorer; the remaining inference pipeline is identical. Local optimization stops at an exchange ratio of 0.005. If a topology cycle is detected, the highest-scoring tree in the cycle is retained.",
    appendixConfigP2: "The DIPPER-paper datasets use the missing-data-aware distance suited to their sparse, coherent coverage; the other panels use the standard distance. DIPPER GPU uses the paper configuration.",
    methodAppendixTitle: "Appendix: method",
    methodOverviewTitle: "B.1 Method overview",
    methodParametersTitle: "Inference parameters",
    methodParametersIntro: 'Let <em>X</em>∈Σ<sup><em>N</em>×<em>L</em></sup> denote an MSA with <em>N</em> taxa and <em>L</em> aligned sites. ConcordTree returns an unrooted binary tree <em>T</em> on the same leaf set. One inference run is specified by the following parameter tuple:',
    parameterTreeTarget: "tree target",
    parameterTreeTargetText: "Selects gene-tree or species-tree inference, with species tree as the default.",
    parameterDistance: "AP-NJ distance",
    parameterDistanceText: "Selects the standard or missing-data-aware distance, with standard as the default.",
    parameterScorer: "quartet scorer",
    parameterScorerText: "For a quartet formed by choosing one taxon from each of the four branches incident to an internal edge, the scorer assigns support to AB|CD, AC|BD, and AD|BC. A higher score means that the topology is more compatible with the sequence patterns in the MSA; NNI uses the score gain of an alternative topology over the current topology. The scorer may be a lightweight MLP or a Transformer, with MLP as the default. View NNI always uses the MLP matched to the tree target.",
    parameterViews: "number of Views",
    parameterViewsText: "Accepts 2–8 and defaults to 4. Each View selects a complementary site subset and ultimately yields a tree containing all N taxa.",
    parameterStops: "refinement controls",
    parameterStopsText: 'For stage <em>z</em>∈{View, Coordinate, Saturation}, ε<sub>z</sub> is the move-ratio threshold and <em>R</em><sub>z</sub> is the round cap. The defaults are (0.01,24), (0.005,4), and (0.005,5), respectively.',
    methodApnjTitle: "AP-NJ: matrix-free aggregate-profile neighbor joining",
    apnjIntro: 'View <em>k</em> selects a site set <em>J</em><sub>k</sub>⊆{1,…,<em>L</em>} while retaining all <em>N</em> taxa. AP-NJ constructs a complete scaffold tree from this submatrix. At site <em>s</em>, each taxon or merged cluster is represented by a four-dimensional state profile; observed nucleotides use one-hot vectors and missing states are handled by distance model <em>g</em>.',
    apnjStandardText: 'Under the standard model, a missing state is represented by the empirical nucleotide frequency <em>q</em><sub>s</sub> at that site. If <em>r</em><sub>is</sub> and <em>r</em><sub>js</sub> are the state profiles of two clusters, the sitewise mismatch distance is',
    apnjStandardExplain: "The inner product is the probability that the two profiles take the same nucleotide state, so one minus this value is their expected mismatch probability.",
    apnjCoverageText: 'Under the missing-data-aware model, <em>p</em><sub>is</sub> is a one-hot vector for an observed nucleotide and the zero vector for a missing cell; <em>m</em><sub>is</sub>=<strong>1</strong><sup>T</sup><em>p</em><sub>is</sub> is its observed mass. Let <em>q</em><sub>s</sub> be the empirical distribution of observed nucleotides at site <em>s</em>, and let π<sub>s</sub>=1−‖<em>q</em><sub>s</sub>‖<sub>2</sub><sup>2</sup> be the mismatch probability of two independent draws from that distribution. If <em>c</em><sub>s</sub> is the observed fraction of taxa at the site, then <em>w</em><sub>s</sub>=<em>c</em><sub>s</sub><sup>2</sup> is the probability that a random taxon pair is jointly observed. Define',
    apnjCoverageFormulaLead: "Here h_is is the shift in the observed state relative to the background mismatch rate. The missing-data-aware site distance is",
    apnjCoverageExplain: "When both endpoints are observed, the expression reduces to ordinary mismatch; when both are missing, it returns the site prior; with one observed endpoint, it interpolates continuously between marginalization and empirical imputation.",
    csiTitle: "Coherent sparsity index",
    csiIntro: 'CSI jointly describes the sparsity of shared observations between taxon pairs and whether taxa tend to miss the same regions. Let <em>S</em><sub>i</sub> be the set of sites observed for taxon <em>i</em>. For each taxon pair, define the shared-coverage proportion <em>O</em><sub>ij</sub> and the Jaccard similarity <em>J</em><sub>ij</sub> of their observation masks:',
    csiDefinitionLead: "The coherent sparsity index (CSI) is",
    csiExplain: "The first factor grows when taxon pairs share few directly observed sites and therefore measures sparsity. The second grows when different taxa observe the same regions and therefore measures coherence. CSI is high only when both properties hold. It thus separates diffuse missingness from blockwise, correlated missingness—the regime modeled by the missing-data-aware distance.",
    csiGuidance: "CSI≤0.25: choose the standard distance. CSI≥0.35: choose the missing-data-aware distance. For 0.25<CSI<0.35, compare both distances.",
    apnjAggregateText: 'Both distances are affine-bilinear in the cluster profiles. Let <em>A</em> be the active cluster set, <em>n</em><sub>A</sub> its size, and <em>P</em><sub>s</sub>=∑<sub><em>j</em>∈<em>A</em></sub><em>p</em><sub>js</sub> the aggregate profile. AP-NJ obtains the standard NJ row sums <em>R</em><sub>i</sub>=∑<sub><em>j</em>∈<em>A</em></sub><em>d</em><sub>g</sub>(<em>i</em>,<em>j</em>) directly from <em>P</em><sub>s</sub>, without materializing a dense <em>N</em>×<em>N</em> distance matrix. Sparse candidate pairs are still ranked by the standard NJ criterion',
    apnjQExplain: "Here d_g(i,j) is the cluster distance chosen by parameter g, and R_i and R_j are the corresponding distance row sums. AP-NJ sparsifies only the search for candidate pairs; candidate distances, row sums, and Q values are evaluated under the selected distance law.",
    algorithmLabel: "Algorithm",
    apnjAlgorithmIO: '<strong>Input:</strong> View submatrix <em>X</em><sub>J</sub> and distance model <em>g</em>. <strong>Output:</strong> a binary tree containing every taxon.',
    apnjAlgorithmSteps: '<li>Initialize one state profile per taxon and set the active cluster set <em>A</em> to all leaves.</li><li>While |<em>A</em>|&gt;3, use profile projections to propose a small neighbor set for each cluster.</li><li>Compute the exact NJ row sum <em>R</em><sub>i</sub> for every active cluster from aggregate profiles.</li><li>Evaluate <em>Q</em>(<em>i</em>,<em>j</em>) on candidate pairs and select disjoint reciprocal-best pairs.</li><li>Merge the selected pairs in parallel; update mean profiles and distance offsets so subsequent distances obey the standard NJ recurrence.</li><li>Connect the final three active clusters and return the tree.</li>',
    methodMergeTitle: "Compatible split merge",
    mergeIntro: 'Represent each View tree <em>T</em><sub>k</sub> by its set of nontrivial bipartitions <em>S</em>(<em>T</em><sub>k</sub>). ConcordTree first forms the split bank 𝒮=⋃<sub><em>k</em>=1</sub><sup><em>K</em></sup><em>S</em>(<em>T</em><sub>k</sub>). The View support count of split <em>s</em> is',
    mergeSupportExplain: "The equality c(s)=K means that every View supports the split. Remaining splits are ordered by whether their local neural gain is nonnegative in every available View, then by support count, median gain, and mean gain.",
    mergeCompatibilityText: 'Two splits <em>A</em>|<em>A</em><sup>c</sup> and <em>B</em>|<em>B</em><sup>c</sup> are compatible if and only if at least one of the following four intersections is empty:',
    mergeCompatibilityExplain: "This condition guarantees that both bipartitions can occur in one unrooted tree. Splits are accepted in evidence order, yielding a maximal compatible set under that ordering; the procedure does not claim to solve a globally weighted optimal-tree problem.",
    mergeMedoidText: "To provide a consistent anchor for unresolved regions, define the medoid View as",
    mergeMedoidExplain: "Here △ is the symmetric difference, whose size counts the splits on which two trees differ. The medoid View therefore minimizes total split distance to the other Views; it supplies anchor splits only and never overrides an accepted compatible split.",
    mergeAlgorithmName: "compatible split merge",
    mergeAlgorithmIO: '<strong>Input:</strong> View trees <em>T</em><sub>1</sub>,…,<em>T</em><sub>K</sub> and their split evidence. <strong>Output:</strong> consensus tree <em>T</em><sub>0</sub>.',
    mergeAlgorithmSteps: '<li>Extract the canonical bipartitions from all View trees and build the split bank.</li><li>Insert every unanimous split with c(s)=K into set 𝒞.</li><li>Order all remaining splits by stability, View support, and neural evidence.</li><li>Scan the ordered splits; add a candidate exactly when it is compatible with every split already in 𝒞.</li><li>Add compatible anchor splits from the medoid View.</li><li>Complete 𝒞 deterministically until it contains N−3 internal edges, then realize it as T₀.</li>',
    methodNniTitle: "Three NNI refinements",
    nniIntro: 'For an internal edge <em>e</em>, denote its four incident branches by <em>A</em>, <em>B</em>, <em>C</em>, and <em>D</em>. Retaining the current topology and performing either NNI exchange gives three local states: AB|CD, AC|BD, and AD|BC. The quartet scorer supplies the potentials',
    nniGainText: 'Here <em>m</em> is the selected scorer. If the current state is τ<sub>0</sub> and the best alternative is τ<sup>*</sup>, the exchange gain is Δ(<em>e</em>)=<em>L</em><sub>m</sub>(τ<sup>*</sup>)−<em>L</em><sub>m</sub>(τ<sub>0</sub>). Each round applies only stage-eligible positive-gain NNIs that share no internal node, preventing simultaneous edits of adjacent edges.',
    viewNniText: "Examines every internal edge of each AP-NJ tree. The initial topology comes from that View's site subset, but NNI scoring reads the full MSA. It draws a few nearby representatives from each of the four incident branches and uses the lightweight scorer to remove clear local errors.",
    coordinateNniText: "Starting from T₀, it examines only edges that lack unanimous View support. One context panel serves several nearby edges, and two coverage perspectives jointly verify each edge, enabling batched repair of the consensus tree.",
    saturationNniText: "It re-identifies uncertain edges after Coordinate NNI and assigns each edge a dedicated context balanced across its four incident branches. Both total gain and the median representative-quartet gain must be positive, concentrating computation on the remaining edges.",
    nniStopText: 'Let <em>M</em><sub>r</sub> be the accepted exchange set in round <em>r</em>. An unrooted binary tree with <em>N</em> leaves has <em>N</em>−3 internal edges, so define the normalized exchange ratio',
    nniStopExplain: 'Stage <em>z</em> stops when no eligible exchange remains, ρ<sub>r</sub>≤ε<sub>z</sub>, or <em>r</em>≥<em>R</em><sub>z</sub>. The threshold ε<sub>z</sub> controls convergence depth and <em>R</em><sub>z</sub> bounds worst-case runtime. Either control may be disabled individually, but not both.',
    nniAlgorithmName: "unified NNI refinement",
    nniAlgorithmIO: '<strong>Input:</strong> tree <em>T</em>, stage <em>z</em>, scorer <em>m</em>, and stopping controls (ε<sub>z</sub>,<em>R</em><sub>z</sub>). <strong>Output:</strong> the refined tree.',
    nniAlgorithmSteps: '<li>If z=View, target every internal edge of T; otherwise target the current edges without unanimous View support.</li><li>Construct representative-taxon context according to stage z and evaluate the three local-topology potentials for each target edge.</li><li>Order positive-gain candidates by gain and select a conflict-free NNI set.</li><li>Apply the selected NNIs simultaneously, obtain the next-round tree, and compute ρ<sub>r</sub>.</li><li>If there is no move, ρ<sub>r</sub>≤ε<sub>z</sub>, or <em>R</em><sub>z</sub> is reached, return the current tree; otherwise rebuild context and continue.</li>',
    nniSchedule: '<strong>Schedule:</strong> each AP-NJ tree first undergoes View NNI, producing T₁,…,T<sub>K</sub>. Their merge gives T₀, which then passes through Coordinate NNI and Saturation NNI to produce the final tree T*.',
    localOnly: "Local preview · not published"
  }
};

function setLanguage(language) {
  const lang = language === "en" ? "en" : "zh";
  document.documentElement.lang = lang === "zh" ? "zh-CN" : "en";
  document.title = lang === "zh" ? "ConcordTree · 测试结果" : "ConcordTree · Results";
  document.querySelectorAll("[data-i18n]").forEach((node) => {
    const value = translations[lang][node.dataset.i18n];
    if (value !== undefined) node.textContent = value;
  });
  document.querySelectorAll("[data-i18n-html]").forEach((node) => {
    const value = translations[lang][node.dataset.i18nHtml];
    if (value !== undefined) node.innerHTML = value;
  });
  document.querySelectorAll("[data-lang]").forEach((button) => button.classList.toggle("active", button.dataset.lang === lang));
  document.getElementById("readme-link").href = lang === "zh" ? "../README.zh-CN.md" : "../README.md";
  localStorage.setItem("concordtree-language", lang);
}

document.querySelectorAll("[data-lang]").forEach((button) => button.addEventListener("click", () => setLanguage(button.dataset.lang)));
const savedLanguage = localStorage.getItem("concordtree-language");
setLanguage(savedLanguage || "zh");

window.addEventListener("load", () => {
  const anchor = decodeURIComponent(window.location.hash.slice(1));
  const target = anchor ? document.getElementById(anchor) : null;
  if (target) window.requestAnimationFrame(() => target.scrollIntoView({ block: "start" }));
});
